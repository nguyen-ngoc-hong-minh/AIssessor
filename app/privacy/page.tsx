import type { Metadata } from "next";
import { SiteHeader } from "@/components/site-header";
import { SiteFooter } from "@/components/site-footer";
export const metadata:Metadata={title:"Privacy"};
export default function PrivacyPage(){return <><SiteHeader/><main className="section public-content"><span className="kicker">Privacy</span><h1>Your work description should not become somebody else’s training data.</h1><p className="lead">This policy is a product-ready draft and must be reviewed by counsel before commercial launch.</p><div className="prose"><h2>Identity</h2><p>BENCHFLOW uses the verified ChatGPT identity supplied by the hosting platform and stores the associated email and profile data in Convex, never account passwords.</p><h2>Planner data</h2><p>Work descriptions are sent to the configured OpenAI Responses API project to generate a structured workflow. Provider retention controls depend on the configured business account and endpoint.</p><h2>Model evidence</h2><p>Artificial Analysis and OpenRouter data are stored as dated source snapshots. They do not receive private user workflow content during routine synchronisation.</p><h2>Payments</h2><p>Stripe processes payment details. BENCHFLOW stores customer, subscription, plan, and status identifiers required for access enforcement.</p><h2>Deletion</h2><p>Account-data deletion is handled by a verified support workflow. Related-record retention and deletion should be finalized before launch.</p></div></main><SiteFooter/></>}
